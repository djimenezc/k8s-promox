# Stack-local entrypoint: `cd k8s-promox/terraform && make tofu-plan`.
# State key is derived from this stack's path by Makefile.tofu.
# Same targets are also reachable from this repo's root Makefile.
TF_DIR := .
include ../../Makefile.tofu
